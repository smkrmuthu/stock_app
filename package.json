{
  "name": "stock-market-dashboard",
  "version": "1.0.0",
  "description": "Professional Stock Market Trading Dashboard — Extensible Architecture",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-market-data.js",
    "dev": "concurrently -n api,vite -c blue,green \"node server.js\" \"vite\"",
    "dev:frontend": "vite",
    "dev:api": "node server.js",
    "build": "node scripts/sync-market-data.js && vite build && cp dist/index.html dist/404.html",
    "preview": "vite preview",
    "deploy": "node scripts/sync-market-data.js && vite build && cp dist/index.html dist/404.html && gh-pages -d dist --user \"Muthu Kumar <smkrmuthu@gmail.com>\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "gh-pages": "^6.3.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
